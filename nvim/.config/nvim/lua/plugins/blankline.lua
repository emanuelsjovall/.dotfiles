return {
  "lukas-reineke/indent-blankline.nvim",
  enabled = true,
  main = "ibl",
  opts = {},
  -- main + opts same as this (main is what tells lazy to require("main") etc)
  -- config = function()
  --     require("ibl").setup()
  -- end
}
