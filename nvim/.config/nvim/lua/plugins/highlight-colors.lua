return {
    'brenoprata10/nvim-highlight-colors',
    enabled = true,
    -- this is the same as just opts = {}
    -- config = function()
    --     require('nvim-highlight-colors').setup({})
    -- end,
    opts = {},
}
